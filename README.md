A cli option for cscope

```sh
# Setup the development envirnoment (virtualenv)
python -m venv venv
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt

# Run the program
python main.py
```
